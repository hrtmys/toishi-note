<button type="button" class="btn btn-outline-secondary btn-sm" data-controller="todos-copy" data-todos-copy-url-value="<%= url %>" data-action="click->todos-copy#copy">
  <i class="bi bi-clipboard me-1"></i><%= label %>
</button>
