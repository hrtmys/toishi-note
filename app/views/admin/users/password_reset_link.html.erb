<%= render partial: "one_time_link", locals: {
  heading: t("admin.users.password_reset_link.heading", email: @user.email_address),
  description: t("admin.users.password_reset_link.description"),
  url: @reset_url,
} %>
